specVersion: "v1"
serviceId: "lumzenmind"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lumzenmind-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
